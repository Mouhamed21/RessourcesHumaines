{% extends 'base.html.twig' %}

{% block body %}
    <h1>EvaluationEmploye</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ evaluationEmploye.id }}</td>
            </tr>
            <tr>
                <th>Noteevaluation</th>
                <td>{{ evaluationEmploye.noteEvaluation }}</td>
            </tr>
            <tr>
                <th>Observation</th>
                <td>{{ evaluationEmploye.observation }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('evaluation_evaluationemploye_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('evaluation_evaluationemploye_edit', { 'id': evaluationEmploye.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
